import { ScaleFontSizeDirective } from './scale-font-size.directive';

describe('ScaleFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
